node_modules
.next
.env
*.tsbuildinfo
next-env.d.ts


.vercel

# 環境変数（追加）
.env.local
.env.production
.env.staging
.env.vercel
.env.*.local

# PayloadCMS
/media/
.payload/

# Secrets ディレクトリ
.secrets/

# DB backup dumps (contains encrypted secrets)
scripts/backups/

# E2E / ローカル検証で取得する Payload の session cookie
pl-cookie.txt

# Payload CMS auto-generated (re-created on dev server start / build)

# Rehearsal-specific env files
.env.rehearsal
*.rehearsal

# Temporary working dir (rehearsal artifacts, CSV, point migration JSON, etc.)
# These contain potentially sensitive customer data and must never be committed.
/tmp/
